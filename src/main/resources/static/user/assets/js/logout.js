document.addEventListener("DOMContentLoaded", function() {
    var xoaCookieButton = document.getElementById("xoaCookieButton");
    
    xoaCookieButton.addEventListener("click", function() {
        // Đặt thời gian hết hạn của cookie thành một thời điểm trong quá khứ

        // Thông báo cho người dùng rằng cookie đã được xóa
        

        // Tùy chỉnh hành vi sau khi xóa cookie (nếu cần)
    });
});